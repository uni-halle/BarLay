#ifndef _TYPES_
#define _TYPES_

#include "params.hu"

#include <stdint.h>

namespace layout {
    struct Position {
        uint32_t i_schedule;
    };

    struct Layout {
        struct Position positions[ROW_COUNT][COL_COUNT];
    };
}

namespace barcodes {
    struct Schedule {
        uint32_t chunks[SYNTH_SCHEDULE_CHUNKS];
    };

    using ScheduleSet = struct Schedule[NUMBER_OF_SCHEDULES];
}

#endif